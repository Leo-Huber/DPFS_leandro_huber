<body>
  <header>
<nav>
  <a href="/products">Productos</a>
<a href="/cart">
  Carrito
<% if (session && session.cart && session.cart.items && session.cart.items.length > 0) { %>
  <span class="cart-badge"><%= session.cart.items.length %></span>
<% } %>
</a>
  <% if (user) { %>
    <a href="/users/profile">Perfil (<%= user.firstName %>)</a>
    <a href="/users/logout">Cerrar sesión</a>
  <% } else { %>
    <a href="/users/register">Registro</a>
    <a href="/users/login">Login</a>
  <% } %>
</nav>

  </header>
